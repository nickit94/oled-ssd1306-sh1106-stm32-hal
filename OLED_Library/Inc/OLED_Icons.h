#ifndef ICONS_H_
#define ICONS_H_

#include "main.h"

#define NULL_ICON (void*)0

/* Иконки в формате XBM */
extern const uint8_t icon_oil[];			// Иконка масло (капля)
extern const uint8_t icon_ab[];				// Иконка траектории от А до В
extern const uint8_t icon_clock[];			// Иконка таймера
extern const uint8_t icon_spark_plug[];		// Иконка свечи зажигания
extern const uint8_t icon_dialy_odo[];		// Иконка суточного одометра
extern const uint8_t icon_reset[];			// Иконка сброса счетчика
extern const uint8_t icon_sound_off[];		// Иконка динамика с крестиком
extern const uint8_t icon_sound_on[];		// Иконка динамика
extern const uint8_t icon_save[];			// Иконка дискеты
extern const uint8_t icon_exit[];			// Иконка выхода

#endif /* ICONS_H_ */
